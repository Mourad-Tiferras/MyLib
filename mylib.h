#ifndef MYLIB_H
#define MYLIB_H

#include <unistd.h>
#include <stdlib.h>
#include <string.h>
#include <string>


char    *_itoa(int   num);
char	*_substr(char *s, unsigned int start, size_t len);


#endif